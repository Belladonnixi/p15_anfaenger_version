import 'package:flutter/material.dart';
// import 'package:p15_anfaenger_version/listview_builder_version.dart';// diese Zeile auskommentieren um die ListView.builder Version zu verwenden
import 'package:p15_anfaenger_version/without_list_view.dart';
// import 'package:p15_anfaenger_version/listview_version.dart'; // diese Zeile auskommentieren um die ListView.builder Version zu verwenden

// Die Hauptfunktion, die die App startet.
void main() {
  // MainApp ist die Klasse, die die ListView Version enthält hier also ändern auf MainApp
  // MainApp ist die Klasse, die die ListView Version enthält hier also ändern auf MainApp
  // wenn man die ausführen will
  runApp(const MainApp3());
}
